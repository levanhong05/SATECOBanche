<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dfm.honglv.satecobanche">

    <uses-sdk android:minSdkVersion="14" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- API key for the Android Maps API v2. The value is defined as a string resource. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAkm1_YF-r7JsvdxBjblI1zQxL4c4vU4zM" />

        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <activity
            android:name=".main.FormWorkActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="com.dfm.honglv.satecobanche.main.FormworkActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.main.SplashActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".widgets.PressureLineChartActivity" />
        <activity android:name=".widgets.DistanceLineChartActivity" />
        <activity android:name=".widgets.AngleLineChartActivity" />

        <activity
            android:name=".functions.AddConstructionActivity"
            android:theme="@style/AppTheme.PopupWindowTheme" />
        <activity android:name=".navigation.ViewConstructionRecordActivity" />
        <activity android:name=".navigation.ViewConstructionDetailsActivity" />

        <service
            android:name=".main.USBService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".navigation.SettingsActivity"
            android:parentActivityName=".main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dfm.honglv.satecobanche.main.MainActivity" />
        </activity>
        <activity android:name=".main.ChooseFormWorkActivity" />
        <activity
            android:name=".navigation.InformationActivity"
            android:parentActivityName=".main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dfm.honglv.satecobanche.main.MainActivity" />
        </activity>
        <activity
            android:name=".functions.AddFormWorkActivity"
            android:theme="@style/AppTheme.PopupWindowTheme" />

        <activity
            android:name=".functions.AddChipActivity"
            android:theme="@style/AppTheme.PopupWindowTheme" />
    </application>

</manifest>